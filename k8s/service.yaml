apiVersion: v1
kind: Service
metadata:
  name: basic-gitops-service
  namespace: gitops-basic
spec:
  type: ClusterIP
  selector:
    app: basic-gitops-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
